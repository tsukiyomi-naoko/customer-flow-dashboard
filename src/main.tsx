import React from "react";
import ReactDOM from "react-dom/client";
import CustomerFlowDashboard from "./App";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <CustomerFlowDashboard />
  </React.StrictMode>
);
